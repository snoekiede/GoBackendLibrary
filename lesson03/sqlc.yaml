version: "2"
sql:
    - engine: "postgresql"
      schema: "db/migrations/"
      queries: "db/queries/"
      gen:
        go:
          out: "internal/database"
          package: "db"
          emit_json_tags: true
          emit_prepared_queries: true
          emit_interface: true
          sql_package: "pgx/v5"